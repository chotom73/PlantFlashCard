package com.yunjae.plantflashcard.service

import com.yunjae.plantflashcard.dto.Plant

class PlantService {
    fun parsePlantFromJsonData(difficulty: String?): List<Plant>? {
        // declare our return type
        val allPlants = ArrayList<Plant>()

        // Open connection to data feed

        // Parse to plant objects

        // Add plant objects a collection

        // return the collection
        return allPlants
    }
}